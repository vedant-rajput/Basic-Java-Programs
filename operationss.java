public class operationss {
    public static void main(String args[]){
        int a = 10;
        int b = 5;
        System.out.println("add = " + (a+b));
        System.out.println("sub = " + (a-b));
        System.out.println("multi = " + (a*b));
        System.out.println("div = " + (a/b));
        System.out.println("remi = " + (a%b));
        
    /*
     pre increment: the value is first increased by
     +1 then used.(same for the pre decrement value is decreased by -1)

     int a = 10
     int b = ++a;

     post increment: the value is first used then increased by
     +1 then used.(same for the post decrement value is decreased by -1)
            
     int a = 10;
     int b = a++;
         */

    /*
     logical operators:
             
        logical and --> &&
        logical or --> ||
        logical not --> !

      assignment operator:
     
      A = A+10 can be typed as A+ = 10
      B = B-5 can be typed as B- = 5 
     */     
            

         
    }
}
